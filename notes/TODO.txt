-> optimize returning and copy constructors 
-> optimize parser table with pointers
-> error types into class -- make distinction between syntactics errors 
	(EMPTY_, malformed rules, duplicates in set (once terminals are a set)) 
	returned by From_http and semantic errors returned by Check_errors
-> error ARROW_IN_NON_TERMINALS
